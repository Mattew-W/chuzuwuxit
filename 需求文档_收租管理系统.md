# 收租管理系统 - 前端需求文档

> 给开发用，按这份文档可以直接实现。技术栈：React + TypeScript + Vite + Tailwind CSS v4

---

## 一、业务背景

出租屋房东（用户母亲）每月需要：
1. 录入每间房的水电表读数
2. 自动计算房租+电费+水费+卫生费+网线费
3. 生成每间房的结算单图片，微信发给租客
4. 月末结转，本月底数变成下月上期数
5. 导出 Excel 租金单存档

当前有 21 间房（201-407 + 101铺/102铺），电价 1.3元/度，水价 7元/吨。

---

## 二、技术栈

```
React 18 + TypeScript
Vite (构建)
Tailwind CSS v4 (样式)
Dexie.js (IndexedDB 本地数据库)
xlsx (Excel 读写)
dom-to-image-more (结算单截图)
```

所有数据存在浏览器 IndexedDB 中，不需要后端服务器。

---

## 三、数据模型

### Room（房间）
```typescript
interface Room {
  id: string           // 同 name，主键
  name: string         // 房号，如 "201"、"101铺"
  rent: number         // 月租
  deposit: number      // 押金
  elecNow: number      // 电表本月读数
  elecLast: number     // 电表上月读数
  waterNow: number     // 水表本月读数
  waterLast: number    // 水表上月读数
  hygiene: number      // 卫生费
  network: number      // 网线费
  remarks: string      // 备注
  status: 'active' | 'vacant'  // 在租/空置
  moveInDate: string   // 入住日期
  tenantNote: string   // 租客备注（如"押金400"）
}
```

### MonthSnapshot（月度快照，结转时自动保存）
```typescript
interface MonthSnapshot {
  year: number
  month: number
  rooms: Room[]
  elecPrice: number
  waterPrice: number
  createdAt: string  // ISO 时间戳
}
```

### AppSettings（系统设置）
```typescript
interface AppSettings {
  elecPrice: number       // 电价，默认 1.3
  waterPrice: number      // 水价，默认 7
  defaultHygiene: number  // 新房间默认卫生费，默认 5
  defaultNetwork: number  // 新房间默认网线费，默认 0
}
```

---

## 四、计算逻辑

```typescript
// 每个房间的费用计算
elecUsage  = max(0, elecNow - elecLast)      // 用电度数
elecAmount = elecUsage × elecPrice           // 电费金额
waterUsage = max(0, waterNow - waterLast)    // 用水吨数
waterAmount = waterUsage × waterPrice        // 水费金额
total = rent + elecAmount + waterAmount + hygiene + network  // 合计应收

// 月度总计
grandTotal = sum(所有房间 total)
```

### 结转下月逻辑
1. 把当前月所有房间数据保存为 MonthSnapshot
2. 每个房间：`elecLast = elecNow`、`waterLast = waterNow`、`remarks = ''`
3. 月份 +1

---

## 五、页面结构（单页应用，通过弹窗切换）

### 5.1 主页面

```
┌─────────────────────────────┐
│  ← 2026年5月  →      [⚙️]  │  月份切换 + 设置入口
│  [历史快照]  [结转下月]      │  操作按钮行
├─────────────────────────────┤
│  ┌─ 总收入大横幅 ─────────┐ │
│  │  ¥12,580 / 18户在租    │ │  橙色渐变背景
│  └────────────────────────┘ │
│  [房租 ¥5000] [电费 ¥1200] [水费 ¥350] │  三格统计
├─────────────────────────────┤
│                             │
│  ┌─── 201 ────────── [×]──┐│
│  │房租│电本月│电上月│用电│水本月│水上月│用水│合计│
│  │320 │7218 │7181 │37 │319  │318  │1  │380│
│  │卫生5│电费48│网线0│水费7│押300│备注│   │结算单│
│  └─────────────────────────┘│
│                             │
│  ┌─── 202 ──── 空置 ── [×]─┐│ 空置房半透明
│  │ 0  │7525 │7525 │ - │689  │689  │ - │  5│
│  │卫生5│电费0 │网线0│水费0│押0  │    │   │结算单│
│  └─────────────────────────┘│
│                             │
│  ... 其他房间 ...            │
│                             │
│                    [＋]     │  右下角浮动加号(新增房间)
└─────────────────────────────┘
```

### 5.2 设置页（弹窗）

全屏弹窗，包含：
- **单价设置**：电价、水价、默认卫生费、默认网线费 四个输入框 + 保存按钮
- **导出数据**：两个按钮——导出 Excel 租金单、导出 JSON 备份
- **导入数据**：两个按钮——从 Excel 导入、从 JSON 备份恢复（点击触发文件选择器）

### 5.3 历史快照页（弹窗）

列表显示所有历史月份快照，每项显示：年份月份、房间数、总收入。点击加载到主页面。

### 5.4 结算单页（弹窗）

全屏 iframe 展示结算单 HTML，顶部有分享/保存按钮。调用 `navigator.share()` 分享图片，降级为下载。

---

## 六、UI 设计规范

### 色彩
| 用途 | 色值 |
|------|------|
| 主色（brand） | `#e86a3a` / `#c85830` |
| 页面背景 | `#f5f3f0`（浅暖灰） |
| 卡片背景 | `#ffffff` |
| 卡片表头底 | `#faf8f5`（微暖白） |
| 文字主色 | `#1a1a1a` |
| 文字辅助 | `#888888` |
| 电费相关 | `#d97706`（琥珀色） |
| 水费相关 | `#2563eb`（蓝色） |
| 合计金额 | `#c85830`（橙红） |

### 关键原则
- **表格式布局优先**，不要传统的圆角卡片信息堆叠
- 每个房间卡片 = 微型表格，列对齐才能一眼看清
- 字体用系统字体栈：`-apple-system, PingFang SC, Microsoft YaHei`
- 移动端优先，最大宽度 480px 居中
- 按钮用圆角（`rounded-full`），主按钮用橙色渐变
- 不要渐变背景、阴影、模糊等花哨效果

### iOS / 微信适配
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="theme-color" content="#e86a3a">
```
底部按钮区域需要 `padding-bottom: env(safe-area-inset-bottom)` 避开 iPhone 底部横条。

---

## 七、Excel 格式规范

### 导出格式（必须和原始模板一致）

每个房间占 7 行，格式如下：

| 行 | 内容 |
|----|------|
| 1 | `2026年5月 - 房号 201 结算单`（合并单元格） |
| 2 | `房号` `房租` `电费明细`(合并6列) `水费明细`(合并6列) `卫生` `网线` |
| 3 | `` `` `本月` `上月` `实用` `单价` `金额` `本月` `上月` `实用` `单价` `金额` `` `` |
| 4 | `201` `320` `7218` `7181` `37` `1.3` `48.1` `319` `318` `1` `7` `7` `5` `0` |
| 5 | `备注:` `` `` `` `` `` `合计应收:` `` `` `` `` `380.1` |
| 6 | 空行 |
| 7 | 空行 |

### 导入解析（支持各种变体）

因为用户的 Excel 可能有多种格式，导入时需要灵活解析：
- 找包含"结算单"或"房号"的行来确定房间分界
- 找数字行来提取读数（房租在第3列、电本月第4列、电上月第5列、水本月第9列、水上月第10列、卫生第14列、网线第15列）
- 跳过合计行和表头行

---

## 八、IndexedDB 表结构（Dexie.js）

```typescript
const db = new Dexie('RentalManager')
db.version(1).stores({
  rooms: 'name',              // Room 表，主键 name
  snapshots: '[year+month]',   // MonthSnapshot 表，联合索引
  settings: '',                // AppSettings 表（只存一条）
})
```

---

## 九、关键交互

### 房间卡片
- 点击卡片右上角 `×` 进入删除确认（显示"删"和"否"两个按钮）
- 表头行始终可见（房租/电本月/电上月/用电/水本月/水上月/用水/合计）
- 第一行数据：实际数值
- 第二行细则：卫生费、电费金额、网线费、水费金额、押金、备注、结算单按钮
- 空置房整体透明度 50%

### 月份切换
- 左右箭头切换月份
- 如果该月有历史快照，自动加载
- 如果没有，显示空白（需要手动录入或从快照加载）

### 结转下月
- 保存当前月快照到 IndexedDB
- 所有房间读数结转（本月→上月）
- 月份自动+1

### 结算单图片
- 用 iframe 渲染 HTML 结算单
- dom-to-image-more 截图
- 调用 navigator.share() 分享（iOS 微信支持）
- 降级方案：下载 PNG

### 浮动按钮
- 右下角 "+" 按钮，点击新增房间
- 新房间房号自动递增（从已有最大房号+1）
- 如果房间列表为空，空状态页中间显示"添加房间"按钮

---

## 十、文件结构

```
src/
├── types/index.ts          # 类型定义
├── lib/
│   ├── db.ts               # Dexie 数据库 CRUD
│   ├── calculator.ts       # 费用计算 + 结转逻辑
│   ├── excel.ts            # Excel 导入导出
│   ├── receipt.tsx          # 结算单 HTML 生成
│   └── seed.ts             # 示例数据（21间房）
├── hooks/
│   └── useData.ts          # 核心数据 Hook
├── components/
│   ├── Toast.tsx            # 轻提示
│   ├── StatsBar.tsx         # 顶部统计
│   ├── RoomCard.tsx         # 房间卡片（核心组件）
│   ├── ReceiptModal.tsx     # 结算单弹窗
│   ├── SettingsModal.tsx    # 设置弹窗
│   └── HistoryModal.tsx     # 历史快照弹窗
├── App.tsx                  # 主应用
├── main.tsx                 # 入口
└── index.css                # 全局样式 + Tailwind + 设计 token
```

---

## 十一、注意事项

1. **所有数据存浏览器**，不需要后端，不需要登录
2. **导出 JSON 备份**用于换手机时迁移数据
3. **首次使用自动填入 21 间示例数据**（来自 seed.ts，避免空页面）
4. 电价水价修改后，页面上的计算结果立即更新
5. 生成结算单前确保 iframe 渲染完成再截图
6. iOS Safari 的 `navigator.share()` 支持分享文件，微信内浏览器不支持但可以长按 iframe 保存图片
